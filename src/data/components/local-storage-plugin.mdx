---
title: Local Storage Plugin
description: Local storage data with Alpine JS official $persist plugin.
emoji: 📦
---

This uses the official Alpine JS [$persist](https://alpinejs.dev/plugins/persist) plugin.

You can refresh the browser and Alpine JS will auto populate the `x-data` value from the local storage data.

<Example src="local-storage-plugin" />
